from build123d import *
from ocp_vscode import *

is_horizontal = True
coil_no = 5
coil_spacing = 1
length = 5


gl = GridLocations(x_spacing=1, y_spacing=1, x_count=5, y_count=1)
print("done")
# l = Line()

# xs = [n * coil_spacing for n in range(0, len(coil_no))]

# def get_point(is_start, length, coil_spacing, n):
#     x = 
# get_end_point = lambda is_horizontal, length: (0, length) if is_horizontal else (length, 0)
# l1 = Line((0, 0), get_point(is_horizontal, length))
